<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <!--<t t-extend="calendar_box">
        <div t-att-style="typeof color === 'string' ? ('background-color:' + color) + ';' : ''" t-attf-class="#{record.is_highlighted ? 'o_event_hightlight' : ''} #{typeof color === 'number' ? _.str.sprintf('o_calendar_color_%s', color) : 'o_calendar_color_1'}">
                <div class="o_event_title" t-esc="record.display_name"/>
                <div class="o_event_title" t-esc="record.partner_ids"/>
                <div class="" t-esc="record.fds_attendees_custom"/>
                <div class="" t-esc="record.description"/>
            </div>
    </t>-->
   <!-- <record id="web_calendar" model="ir.ui.view">
        <field name="name">Web Calendar Event</field>
        <field name="model">base</field>
        <field name="inherit_id" ref="web.web_calendar"></field>
        <field name="arch" type="xml">
            <t t-extend="calendar_box">
                <div t-att-style="typeof color === 'string' ? ('background-color:' + color) + ';' : ''" t-attf-class="#{record.is_highlighted ? 'o_event_hightlight' : ''} #{typeof color === 'number' ? _.str.sprintf('o_calendar_color_%s', color) : 'o_calendar_color_1'}">
                    <div class="o_event_title" t-esc="record.display_name"/>
                    <div class="o_event_title" t-esc="record.partner_ids"/>
                    <div class="o_event_title" t-esc="record.fds_attendees_custom"/>
                    <div class="o_event_title" t-esc="record.description"/>
                </div>
            </t>
        </field>
    </record>-->
</odoo>